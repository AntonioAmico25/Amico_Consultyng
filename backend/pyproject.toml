[project]
name = "amico-saas-f41"
version = "0.5.2"
description = "Gate F4.1 — homologação cognitiva controlada do Ecossistema AMICO"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.116,<1",
  "httpx>=0.28,<1",
  "pydantic-settings>=2.10,<3",
]

[project.optional-dependencies]
dev = ["pytest>=8.4,<9"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
